{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://github.com/iztiar/iztiar-core/schemas/controller-config.schema.json",
    "title": "engineApi",
    "description": "the API provided to each service at initialization time",
    "type": "object",
    "properties": {
        "commonName": {
            "type": "string",
            "description": "the common name of the application (getter/setter)"
        },
        "config": {
            "type": "coreConfig",
            "description": "the configuration instance filled-up (for the application) from <configDir>/iztiar.json core configuration file (getter/setter)"
        },
        "packet": {
            "type": "PackageJson",
            "description": "the package instance of the calling '@iztiar/iztiar-core' core module (getter/setter)"
        },
        "pluginManager": {
            "type": "IPluginManager",
            "description": "the IPluginManager interface as instanciated by the core application (getter/setter)"
        },
        "exports": {
            "type": "object [Module: null prototype]",
            "description": "the whole list of the definitions exported by the calling '@iztiar/iztiar-core' core module"
        },
        "service": {
            "type": "coreService",
            "description": "the instance which describes the service from calling '@iztiar/iztiar-core' core module point of view"
        }
    }
}
